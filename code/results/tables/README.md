# Tables

The manuscript currently defines its numeric tables directly in `main.tex` or
through package-local macros. Run `python code/scripts/make_figures.py` and
the package-specific reproduction command before adding generated table files
here.
