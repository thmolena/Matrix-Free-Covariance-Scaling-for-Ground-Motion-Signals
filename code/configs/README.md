# Configurations

This directory is the public configuration entry point for **Matrix-Free Covariance Scaling for Recorded Ground-Motion Signals**.
Existing machine-readable configuration files elsewhere in this package remain
canonical when the reproduction command names them explicitly. Configuration
selection uses development data only; test outcomes are not configuration
inputs.
